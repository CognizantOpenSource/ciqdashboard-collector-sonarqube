spring:
  application:
    name: dashboard-collector
  data:
    mongodb:
      uri: mongodb://idashboard:${spring.data.mongodb.credents}@10.120.100.231/idashboard
      credents: ENC(rwQEK1M5vbHwwhhqx6pBDHn4Ut4IrKHbBUsrB0AUR8XHc8GN/Sf4IRVma96OWroe)


scheduler:
  cron: 0 0/1 * * * *
  enable: true

sonarqube:
  url: "http://10.120.100.231:9000"
  token : "admin"
  username : "admin"

server :
  port: 8087
logging:
  level:
    com.cognizant.dashboard.collectors.sonarqube.clients.SonarClient: OFF

  pattern:
    console: '%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %logger.%M - %msg%n'

jasypt:
  encryptor:
    algorithm: PBEWITHHMACSHA512ANDAES_256
    password: LeapSecurityKey
